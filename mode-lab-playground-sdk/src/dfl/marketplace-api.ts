enum NotificationChannel {
	Preferred = 'PREFERRED',
	Email = 'EMAIL',
	PushNotification = 'PUSH_NOTIFICATION',
	SubtleNotification = 'SUBTLE_NOTIFICATION',
}
// const notify = (heading: string, body: string, channels: )
