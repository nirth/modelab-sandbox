describe("Test suide should", () => {
  it("be sane", () => {
    expect(true).toBe(true);
    expect(false).toBe(false);
    expect(true).not.toBe(false);
    expect(false).not.toBe(true);
  });
});
